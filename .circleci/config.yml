#
#  Globality autogenerated CircleCI configuration
#
#  This file is auto generated with globality-build.
#  You should not make any changes to this file manually
#
#  Any changes made to this file will be overwritten in the
#  next version of the build.
#
#  See: http://github.com/globality-corp/globality-build
#
#

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: ${AWS_ECR_DOMAIN}/globality-build:2020.15.20
      aws_auth:
        aws_access_key_id: ${AWS_ACCESS_KEY_ID}
        aws_secret_access_key: ${AWS_SECRET_ACCESS_KEY}
      environment:
        EXTRA_INDEX_URL: "InjectedDuringRuntime"
        AWS_ECR_DOMAIN: "InjectedDuringRuntime"
        JFROG_AUTH: "InjectedDuringRuntime"
        PYPI_USERNAME: "InjectedDuringRuntime"
        PYPI_PASSWORD: "InjectedDuringRuntime"

deploy_defaults: &deploy_defaults
  working_directory: ~/repo
  docker:
    - image: ${AWS_ECR_DOMAIN}/globality-build:2020.15.20
      aws_auth:
        aws_access_key_id: ${AWS_ACCESS_KEY_ID}
        aws_secret_access_key: ${AWS_SECRET_ACCESS_KEY}
      environment:
        EXTRA_INDEX_URL: "InjectedDuringRuntime"
        AWS_ECR_DOMAIN: "InjectedDuringRuntime"
        JFROG_AUTH: "InjectedDuringRuntime"
        PYPI_USERNAME: "InjectedDuringRuntime"
        PYPI_PASSWORD: "InjectedDuringRuntime"


whitelist: &whitelist
  paths:
    .

version: 2

jobs:
  checkout:
    <<: *defaults

    steps:
      - checkout

      - persist_to_workspace:
          root: ~/repo
          <<: *whitelist

  build_docker:
    <<: *defaults

    steps:
      - attach_workspace:
          at: ~/repo

      - setup_remote_docker:
          docker_layer_caching: true

      - run:
          name: Build Docker
          # install dependencies for loading ecs task definitions
          command: |
            eval $(aws ecr get-login --no-include-email)
            # Build on local machine - set the following env vars.
            # Some env vars are constants, so you can set them in your .bashrc

            # CONSTANT env vars:
            # export AWS_ECR_DOMAIN="308798440167.dkr.ecr.us-east-1.amazonaws.com"
            # export EXTRA_INDEX_URL=https://<USERNAME>:<API_TOKEN>@globality.jfrog.io/globality/api/pypi/pypi/simple
            # export JFROG_AUTH=<USERNAME>:<API_TOKEN>
            # export CIRCLE_BUILD_NUM=0

            # DYNAMIC env vars:
            # export CIRCLE_SHA1=$(git log -1 --format=format:%H)

            export DOCKER_IMAGE_SHA_TAG=$AWS_ECR_DOMAIN/python-library:$CIRCLE_SHA1
            docker build --tag $DOCKER_IMAGE_SHA_TAG \
              --build-arg BUILD_NUM=$CIRCLE_BUILD_NUM \
              --build-arg SHA1=$CIRCLE_SHA1 \
              --build-arg EXTRA_INDEX_URL=$EXTRA_INDEX_URL \
              --build-arg JFROG_AUTH=$JFROG_AUTH .

            # Push from local machine (requires AWS ECR permissions):
            # eval $(botoenv -p development)

            eval $(aws ecr get-login --no-include-email)
            docker push $DOCKER_IMAGE_SHA_TAG

  test:
    <<: *defaults

    steps:
      - attach_workspace:
          at: ~/repo

      - setup_remote_docker
      - run:
          name: Test code
          command: |
            docker create -v /src/microcosm_sagemaker/tests/ --name service_tests alpine:3.4 /bin/true
            docker cp $(pwd)/microcosm_sagemaker/tests service_tests:/src/microcosm_sagemaker/
            eval $(aws ecr get-login --no-include-email)
            docker pull ${AWS_ECR_DOMAIN}/python-library:${CIRCLE_SHA1}
            docker run -it  --volumes-from service_tests ${AWS_ECR_DOMAIN}/python-library:${CIRCLE_SHA1} test


  lint:
    <<: *defaults

    steps:
      - attach_workspace:
          at: ~/repo

      - setup_remote_docker

      - run:
          name: Run Lint
          command: |
            docker create -v /src/microcosm_sagemaker/tests/ --name service_tests alpine:3.4 /bin/true
            docker cp $(pwd)/microcosm_sagemaker/tests service_tests:/src/microcosm_sagemaker/
            eval $(aws ecr get-login --no-include-email)
            docker pull ${AWS_ECR_DOMAIN}/python-library:${CIRCLE_SHA1}
            docker run -it --volumes-from service_tests ${AWS_ECR_DOMAIN}/python-library:${CIRCLE_SHA1} lint

  typehinting:
    <<: *defaults

    steps:
      - attach_workspace:
          at: ~/repo

      - setup_remote_docker

      - run:
          name: Run Typehinting
          command: |
            docker create -v /src/microcosm_sagemaker/tests/ --name service_tests alpine:3.4 /bin/true
            docker cp $(pwd)/microcosm_sagemaker/tests service_tests:/src/microcosm_sagemaker/
            eval $(aws ecr get-login --no-include-email)
            docker pull ${AWS_ECR_DOMAIN}/python-library:${CIRCLE_SHA1}
            docker run -it --volumes-from service_tests ${AWS_ECR_DOMAIN}/python-library:${CIRCLE_SHA1} typehinting

  deploy_jfrog_rc:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - run:
          name: Deploy
          command: |
            echo "[distutils]" > ~/.pypirc
            echo "index-servers =" >> ~/.pypirc
            echo "    pypi " >> ~/.pypirc
            echo >> ~/.pypirc
            echo "[pypi]" >> ~/.pypirc
            echo "repository:https://upload.pypi.org/legacy/" >> ~/.pypirc
            echo "username:$PYPI_USERNAME" >> ~/.pypirc
            echo "password:$PYPI_PASSWORD" >> ~/.pypirc
            echo >> ~/.pypirc
            echo >> ~/.pypirc
            sed  -i '/version \=/s/\".*\"/'"\"0.${CIRCLE_BUILD_NUM}.dev${CIRCLE_BUILD_NUM}\"/" setup.py
            python setup.py register -r pypi
            python setup.py sdist
            twine upload --repository pypi dist/microcosm-sagemaker-${version}.tar.gz

  deploy_pypi:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - run:
          name: Deploy
          command: |
            echo "[distutils]" > ~/.pypirc
            echo "index-servers =" >> ~/.pypirc
            echo "    pypi " >> ~/.pypirc
            echo >> ~/.pypirc
            echo "[pypi]" >> ~/.pypirc
            echo "repository:https://upload.pypi.org/legacy/" >> ~/.pypirc
            echo "username:$PYPI_USERNAME" >> ~/.pypirc
            echo "password:$PYPI_PASSWORD" >> ~/.pypirc
            echo >> ~/.pypirc
            version=$(cat .bumpversion.cfg | awk '/current_version / {print $3}')
            python setup.py register -r pypi
            python setup.py sdist
            twine upload --repository pypi dist/microcosm-sagemaker-${version}.tar.gz
            version=$(python setup.py --version)
            globality-build dependency-update \
                --package-name microcosm_sagemaker \
                --version $version \
                --language python


workflows:
  version: 2

  build-and-release:
    jobs:
      - checkout:
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - build_docker:
          requires:
            - checkout
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - lint:
          requires:
            - build_docker
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - test:
          requires:
            - build_docker
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - deploy_jfrog_rc:
          requires:
            - test
            - lint
            - typehinting
      - typehinting:
          requires:
            - build_docker
          filters:
            # run for all branches and tags
            tags:
              only: /.*/
      - deploy_pypi:
          requires:
            - test
            - lint
            - typehinting
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^[0-9]+(\.[0-9]+)*/


